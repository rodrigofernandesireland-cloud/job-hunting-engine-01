[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    integration: tests that exercise multiple application components
    functional: tests that execute the application through its public CLI
    live: tests that require network or external services (normally skipped)
